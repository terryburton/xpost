Xpost Postscript Compliance
===========================

Notation
--------

 [ ] : not implemented
 [/] : partially implemented
 [X] : implemented

Level 1 operators
-----------------

 [X] setdash
 [X] currentdash
 [X] arcto
 [X] fill with complex paths
 [X] eofill
 [X] clip with complex paths
 [X] eoclip
 [X] setlinejoin
 [X] currentlinejoin
 [X] setlinecap
 [X] currentlinecap
 [X] image (12-bit samples render at the raster's 8-bit depth)
 [X] imagemask
 [X] settransfer
 [X] currenttransfer
 [X] setscreen (a spot procedure that leaves no result loses nothing
     from the caller's stack, where the reference interpreters error)
 [X] currentscreen
 [X] setcolorscreen
 [X] currentcolorscreen
 [X] sethalftone
 [X] currenthalftone
 [X] makefont
 [X] ashow
 [X] widthshow
 [X] awidthshow
 [X] kshow
 [X] stringwidth
 [X] Type-3 Fonts
 [X] font data caching
 [X] definefont for modified font properties (matrix, encoding, etc.)
 [X] base font CharStrings (a face whose program cannot be read
     publishes glyph indices)

 [X] pop
 [X] exch
 [X] dup
 [X] copy
 [X] index
 [X] roll
 [X] clear
 [X] count
 [X] cleartomark
 [X] counttomark
 [X] add
 [X] div
 [X] idiv
 [X] mod
 [X] mul
 [X] sub
 [X] abs
 [X] neg
 [X] ceiling
 [X] floor
 [X] round
 [X] truncate
 [X] sqrt
 [X] atan
 [X] cos
 [X] sin
 [X] exp
 [X] ln
 [X] log
 [X] rand
 [X] srand
 [X] rrand
 [X] array
 [X] length
 [X] get
 [X] put
 [X] getinterval
 [X] putinterval
 [X] aload
 [X] astore
 [X] forall
 [X] dict
 [X] maxlength
 [X] begin
 [X] end
 [X] def
 [X] load
 [X] store
 [X] known
 [X] where
 [X] currentdict
 [X] countdictstack
 [X] dictstack
 [X] string
 [X] anchorsearch
 [X] search
 [X] token
 [X] eq
 [X] ne
 [X] ge
 [X] gt
 [X] le
 [X] lt
 [X] and
 [X] not
 [X] or
 [X] xor
 [X] bitshift
 [X] exec
 [X] if
 [X] ifelse
 [X] for
 [X] repeat
 [X] loop
 [X] exit
 [X] countexecstack
 [X] execstack
 [X] quit
 [X] stop
 [X] stopped
 [X] type
 [X] cvlit
 [X] cvx
 [X] xcheck
 [X] executeonly
 [X] noaccess
 [X] readonly
 [X] rcheck
 [X] wcheck
 [X] cvi
 [X] cvn
 [X] cvr
 [X] cvrs
 [X] cvs
 [X] file
 [X] closefile
 [X] read
 [X] write
 [X] readhexstring
 [X] writehexstring
 [X] readstring
 [X] writestring
 [X] readline
 [X] bytesavailable
 [X] flush
 [X] flushfile
 [X] resetfile
 [X] status
 [X] currentfile
 [X] print
 [X] echo
 [X] save
 [X] restore
 [X] vmstatus
 [X] bind
 [X] usertime
 [X] getenv
 [X] gsave
 [X] grestore
 [X] grestoreall
 [X] setlinewidth
 [X] currentlinewidth
 [X] setlinecap
 [X] currentlinecap
 [X] setlinejoin
 [X] currentlinejoin
 [X] setmiterlimit
 [X] currentmiterlimit
 [X] setflat
 [X] currentflat
 [X] setgray
 [X] currentgray
 [X] setrgbcolor
 [X] currentrgbcolor
 [X] sethsbcolor
 [X] currenthsbcolor
 [X] settransfer
 [X] currenttransfer
 [X] matrix
 [X] initmatrix
 [X] identmatrix
 [X] defaultmatrix
 [X] currentmatrix
 [X] setmatrix
 [X] translate
 [X] scale
 [X] rotate
 [X] concat
 [X] concatmatrix
 [X] transform
 [X] dtransform
 [X] itransform
 [X] idtransform
 [X] invertmatrix
 [X] newpath
 [X] currentpoint
 [X] moveto
 [X] rmoveto
 [X] lineto
 [X] rlineto
 [X] arc
 [X] arcn
 [X] arcto
 [X] curveto
 [X] rcurveto
 [X] closepath
 [X] flattenpath
 [X] charpath
 [X] clippath
 [X] pathbbox
 [X] initclip
 [X] clip
 [X] eoclip
 [X] erasepage
 [X] fill
 [X] eofill
 [X] stroke
 [X] strokepath
 [X] reversepath
 [X] image
 [X] imagemask
 [X] showpage
 [X] copypage
 [X] setfont
 [X] currentfont
 [X] show
 [X] stringwidth
 [X] findfont
 [X] scalefont
 [X] setcharwidth
 [X] = and == (also stack, pstack, =print)
 [X] executive (also prompt, start)
 [X] handleerror (an error out of a procedure-implemented operator
     restores the stacks to their depths at the call; operands the
     procedure had already consumed are not pushed back)
 [X] run
 [X] initgraphics
 [X] nulldevice
 [X] pathforall
 [X] version
 [X] eexec
 [ ] internaldict
 [X] cachestatus
 [X] setcachelimit

Level 2 operators
-----------------

 [X] arct
 [X] rectfill
 [X] rectstroke
 [X] rectclip
 [X] selectfont
 [/] setcolorspace (device spaces, Separation, DeviceN, Indexed,
     Pattern and the CIE-based families, rendered through fixed
     daylight primaries; colour rendering dictionaries are recorded
     and take no part)
 [X] currentcolorspace
 [X] setcolor
 [X] currentcolor
 [X] setpagedevice (OutputDevice, PageSize, HWResolution, OutputFile,
     PageOffset; other keys reach the device as features)
 [X] currentpagedevice
 [X] BeginPage, EndPage (called with the count of pages the device has
     transmitted, EndPage also with the reason -- 0 showpage, 1 copypage,
     2 the device going away -- and its boolean decides whether the page
     is transmitted. A handler that is not executable is refused by
     setpagedevice; one that answers with anything but a single boolean
     is a typecheck. A failing handler is let out of showpage and
     copypage, where a program can catch it, but not out of a device
     handover, which would leave the device impossible to replace.
     copypage ends a page with reason 1 and does not count it, as the
     operator descriptions have it; the reference interpreter collapses
     copypage into showpage, reason 0 and counted, as a LanguageLevel 3
     interpreter may)
 [X] xshow
 [X] yshow
 [X] xyshow
 [X] glyphshow
 [X] colorimage
 [X] image with dict argument (ImageTypes 1, 3 and 4; Interpolate
     smooths the samples of any shape on the direct raster path,
     while a mask still decides each pixel by its nearest sample)
 [X] imagemask with dict argument
 [X] setcolortransfer
 [X] currentcolortransfer
 [X] setblackgeneration (recorded only)
 [X] currentblackgeneration
 [X] setundercolorremoval (recorded only)
 [X] currentundercolorremoval
 [X] setcolorrendering (recorded only)
 [X] currentcolorrendering
 [X] Named Resources
 [X] makepattern (every tile repaints its cell; text through a CMap
     composite under a pattern colour paints the underlying colour)
 [X] setpattern
 [X] filter (decode filters: ASCIIHex, ASCII85, SubFile, RunLength,
     Flate, LZW, CCITTFax, DCT, ReusableStream; encode filters:
     ASCIIHex, ASCII85, Null, RunLength, Flate, LZW, CCITTFax, DCT.
     A data source or target may be a string, a file or a procedure,
     and a procedure is called as the filter wants data or needs to be
     rid of it)
 [X] defineresource, findresource, resourcestatus, resourceforall,
     undefineresource (an instance is held to the type its category
     takes and left read-only, as the language requires)
 [ ] resource discovery by filename (no GenericResourceDir or
     GenericResourcePathSep system parameter, and a category
     implementation dictionary carries no ResourceFileName, so a program
     cannot ask where a resource would be installed or found. Instances
     reach the interpreter through its own search path instead)
 [X] Trapping procedure set: settrapparams, currenttrapparams, settrapzone
     (parameters recorded and queryable; no traps are generated, as the
     language leaves in-RIP trapping device-dependent)
 [X] setglobal, currentglobal, gcheck
 [X] gstate, setgstate, currentgstate
 [X] packedarray (built as ordinary arrays), setpacking,
     currentpacking
 [X] setcmykcolor, currentcmykcolor
 [X] setoverprint, currentoverprint (recorded only)
 [X] setsystemparams, currentsystemparams, setuserparams,
     currentuserparams, setdevparams, currentdevparams (accepted;
     the parameter sets are minimal, and a user parameter set inside a
     save bracket keeps the value it was given past the restore that
     ends the bracket, where a user parameter is defined to revert with
     it)
 [X] deletefile, renamefile, filenameforall
 [X] fileposition, setfileposition
 [X] cleardictstack
 [X] undef
 [X] vmreclaim
 [X] realtime
 [X] product, revision, serialnumber
 [X] languagelevel
 [X] undefinefont
 [X] rootfont
 [X] startjob (job boundaries accepted; no VM encapsulation)
 [X] setcachedevice2 (reduces to setcachedevice)
 [X] setcacheparams, currentcacheparams
 [X] user paths (upath, uappend, ufill, ueofill, ustroke,
     ustrokepath, setbbox; ucache is accepted, nothing is cached)
 [X] insideness testing (infill, ineofill, instroke, inufill,
     inueofill, inustroke; a user path without a leading setbbox is
     accepted rather than rejected)
 [X] user objects (defineuserobject, execuserobject,
     undefineuserobject)
 [X] execform (output cached on raster pages, keyed by the form and
     the transformation; a vector or screening page, or an
     unwritable dictionary, paints afresh)
 [X] setstrokeadjust, currentstrokeadjust (recorded only)
 [X] shared VM (setshared, currentshared, scheck, shareddict,
     SharedFontDirectory, GlobalFontDirectory: the global-VM view
     by other names)
 [X] binary object sequences (setobjectformat, currentobjectformat,
     printobject, writeobject; the native-real formats carry their
     reals in the sequence's byte order, as deployed writers do)

Level 3 operators
-----------------

 [X] shfill (sampled functions at byte depths; FunctionTypes 0, 2 and
     3 -- the type 4 calculator functions rangecheck, and functions of
     more than one input are served by type 0 only)
 [X] composite fonts (FMapTypes 2 through 8 over any descendants,
     and FMapType 9 through composefont with CIDFont descendants of
     CIDFontType 2, incremental GlyphDirectory delivery, and
     CIDFontType 0, charstrings through StartData; escapes select
     within one modal level; cshow rebinds a CMap composite to
     one-byte codes)
 [X] clipsave
 [X] cliprestore
 [X] setsmoothness, currentsmoothness (recorded only)
 [X] findcolorrendering (names the default instance)

Extensions
----------

 [X] pdfmark (DOCINFO reaches the pdfwrite device; other mark types
     are consumed without effect)
 [X] page-size operators of the printer products (letter, legal, a4,
     and the rest of the common set)

Display Postscript
------------------

 [X] multiple contexts
 [/] multiple devices (setpagedevice switches the device mid-job; one
     device per context at a time)

NeWS
----

 [X] getenv/putenv
 [X] magic dictionaries
 [ ] setrasteropcode
 [ ] currentrasteropcode
 [ ] classes
 [ ] canvases
 [ ] process type objects (magic dictionary controlling a DPS context)

Typical output devices
----------------------

 [X] pgm
 [X] ppm
 [X] pbm
 [X] tiff (24-bit rgb, one uncompressed strip)
 [X] jpeg
 [X] png (also a png with alpha)
 [X] pdf (pdfwrite)
 [X] svg (svgwrite)
 [X] bounding-box
 [X] dsc-compliant distilled ps (pswrite; separations arrive as
     their process colours)
 [X] xcb
 [X] ms-windows

Known behavioural deviations
----------------------------

 [X] filter  a procedure that is the data target of a filter may not write to
     that same filter: the string the waiting call must give back is the one
     this call would have to return first, so the write is an ioerror. A data
     source is under no such restriction and may read the stream it supplies.
 [X] filter  a data source procedure that re-enters its own stream more than
     two levels deep still delivers every byte it supplied, once, in the order
     the stream has them, where some interpreters drop or repeat bytes past
     that depth and do not agree with each other on which.
 [X] vmstatus  a count of bytes too large for the integer type is reported as
     a real, where the operator is defined to answer three integers. Both
     banks together span more bytes than the type counts, and an integer
     that has wrapped reports the count as small exactly where it is
     largest; a real reports the size and loses the last digits of it. The
     same answer this interpreter gives an arithmetic result that leaves
     the integer range.
 [X] SubFileDecode  a zero count with an empty EOD string passes zero bytes
     (PLRM), where some interpreters read to end-of-file.
 [X] ]  a construction closed over more objects than an array can hold is
     limitcheck, where some interpreters return an array shorter than the
     objects they were given.
 [X] >>  a construction closed over more pairs than a dictionary can hold is
     limitcheck, where some interpreters return a dictionary shorter than the
     pairs they were given.
 [X] error recovery  an operator that refuses hands its caller the operand
     stack as it stood when the operator was called (PLRM 3.11.1) -- the values
     it held, not merely the depth it stood at. Where the operator had run a
     procedure of the program's, and that procedure left values of its own or
     took values from beneath the call, none of that shows through; some
     interpreters put the depth back and leave the procedure's leavings, and
     the slots it emptied, on show underneath.

 Reals are IEEE single precision (PLRM 3.3.2 permits single or double): results
 carry about seven significant figures and large values print in exponent form
 (4e+09).

Implementation limits
---------------------

 Arrays, strings and dictionaries hold 65,535 elements (PLRM Appendix B.1);
 the large-object build carries them to memory instead. Past either, limitcheck.

 There is a second limit on composites that Appendix B does not name, and a
 program inside every limit that appendix does list can still reach it: how
 many composites may exist at once. Every string, array and dictionary takes
 one number from a table however large it is, and there are 524,287 numbers
 (2,147,483,647 in the large-object build) of which the last thousand are not
 the program's: they are what the interpreter reports the exhaustion with.
 Past that, limitcheck -- vmstatus says nothing about this limit, and the
 "maximum" it reports is not this ceiling.

 A composite the program can no longer reach gives its number back when the
 collector next runs, so what the limit counts is composites held at once,
 not composites made. Not all of them are the program's own: a gsave and its
 grestore spend about four numbers between them, released like any other
 garbage, where a save and its restore spend none. `1183615869 internaldict
 /.vmentcount get exec` reports how many numbers each of local and global VM
 has handed out.
