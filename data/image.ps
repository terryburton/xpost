/QUIET where { pop }{ (loading image.ps...\n)print } ifelse

(/pgmimage.ps) //.xpostsys /.loadmodule get exec
(/ppmimage.ps) //.xpostsys /.loadmodule get exec

/QUIET where { pop }{ (eof image.ps\n)print } ifelse
