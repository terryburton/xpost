
option('freetype',
  type: 'feature',
  value: 'enabled',
  description: 'render face-backed text through the freetype library; disabled builds a font system with no faces, whose findfont answers invalidfont while procedural (Type 3) fonts still render'
)

option('doc',
  type: 'boolean',
  value: true,
  description: 'build documentation with Doxygen'
)

option('debug-hooks',
  type: 'boolean',
  value: false,
  description: 'compile in the switches that take a guarantee away from a run'
)

option('large-object',
  type: 'boolean',
  value: false,
  description: 'use of large objects'
)

option('mmap',
  type: 'boolean',
  value: true,
  description: 'back virtual memory with a mapping rather than with the allocator, where the host offers mmap'
)

option('mremap',
  type: 'boolean',
  value: true,
  description: 'grow a mapped arena in place where the host offers mremap; turning this off takes the reservation the hosts without mremap take, which is how that arrangement is exercised on a host that has it'
)

option('valgrind',
  type: 'boolean',
  value: false,
  description: 'run unit tests with valgrind'
)

option('valgrind-arena',
  type: 'boolean',
  value: false,
  description: 'describe virtual memory to memcheck, so a read of storage the interpreter has reclaimed is reported'
)
